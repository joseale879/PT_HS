const router=require('express').Router();const {asyncHandler}=require('../shared/http');const {authenticate}=require('./middleware/auth');const {createRequirePermission}=require('./middleware/requirePermission');const {PostgresPermissionChecker}=require('../core/infrastructure/security/PostgresPermissionChecker');const {ListUserRoles,AssignUserRole,RemoveUserRole}=require('../core/application/use-cases/role/RoleOperations');const {PostgresRoleRepository}=require('../core/infrastructure/repositories/postgres/PostgresRoleRepository');const {RoleController}=require('./controllers/RoleController');const repository=new PostgresRoleRepository();const controller=new RoleController({listUserRoles:new ListUserRoles({repository}),assignUserRole:new AssignUserRole({repository}),removeUserRole:new RemoveUserRole({repository})});const requireRolesManage=createRequirePermission({permissionChecker:new PostgresPermissionChecker()})('roles.manage');router.use(authenticate,requireRolesManage);router.get('/users/:userId',asyncHandler((q,s)=>controller.list(q,s)));router.post('/users/:userId',asyncHandler((q,s)=>controller.assign(q,s)));router.delete('/users/:userId',asyncHandler((q,s)=>controller.remove(q,s)));module.exports=router;
