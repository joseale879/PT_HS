const router=require('express').Router();const {asyncHandler}=require('../shared/http');const {authenticate}=require('./middleware/auth');const {createRequirePermission}=require('./middleware/requirePermission');const {PostgresPermissionChecker}=require('../core/infrastructure/security/PostgresPermissionChecker');const {CreateGoal}=require('../core/application/use-cases/goal/CreateGoal');const {ListGoals}=require('../core/application/use-cases/goal/ListGoals');const {GetGoal}=require('../core/application/use-cases/goal/GetGoal');const {UpdateGoal}=require('../core/application/use-cases/goal/UpdateGoal');const {DeleteGoal}=require('../core/application/use-cases/goal/DeleteGoal');const {GetGoalProgress}=require('../core/application/use-cases/goal/GetGoalProgress');const {PostgresGoalRepository}=require('../core/infrastructure/repositories/postgres/PostgresGoalRepository');const {GoalController}=require('./controllers/GoalController');const repository=new PostgresGoalRepository();const controller=new GoalController({createGoal:new CreateGoal({repository}),listGoals:new ListGoals({repository}),getGoal:new GetGoal({repository}),updateGoal:new UpdateGoal({repository}),deleteGoal:new DeleteGoal({repository}),getGoalProgress:new GetGoalProgress({repository})});const requireManage=createRequirePermission({permissionChecker:new PostgresPermissionChecker()})('homes.manage');router.use(authenticate);router.post('/',requireManage,asyncHandler((q,s)=>controller.create(q,s)));router.get('/',asyncHandler((q,s)=>controller.list(q,s)));router.get('/:goalId/progress',asyncHandler((q,s)=>controller.progress(q,s)));router.get('/:goalId',asyncHandler((q,s)=>controller.get(q,s)));router.put('/:goalId',requireManage,asyncHandler((q,s)=>controller.update(q,s)));router.delete('/:goalId',requireManage,asyncHandler((q,s)=>controller.remove(q,s)));module.exports=router;
