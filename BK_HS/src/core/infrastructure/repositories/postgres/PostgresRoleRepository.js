const {withTransaction}=require('../../../../infrastructure/db');
class PostgresRoleRepository { map(r){return {roleId:r.role_id,roleName:r.role_name,description:r.description,status:r.status};} async listUserRoles({userId,targetUserId}){const q=await withTransaction(userId,c=>c.query('SELECT * FROM user_account.fn_get_user_roles($1::uuid)',[targetUserId]));return q.rows.map(r=>this.map(r));} async assign({userId,targetUserId,roleName}){const q=await withTransaction(userId,c=>c.query('SELECT * FROM user_account.fn_assign_user_role($1::uuid,$2::varchar)',[targetUserId,roleName]));return this.map(q.rows[0]);} async remove({userId,targetUserId,roleName}){const q=await withTransaction(userId,c=>c.query('SELECT user_account.fn_remove_user_role($1::uuid,$2::varchar) AS removed',[targetUserId,roleName]));if(!q.rows[0]?.removed){const e=new Error('El usuario no tiene ese rol');e.status=404;throw e;}} }
module.exports={PostgresRoleRepository};
