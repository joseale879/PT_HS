class GetVacationMode { constructor({repository}){this.repository=repository;} execute({userId,homeId,date}){if(!userId||!/^[0-9a-f-]{36}$/i.test(homeId))throw this.bad('Datos de hogar inválidos');if(date!==undefined&&!this.date(date))throw this.bad('date no es válida');return this.repository.findByHome({userId,homeId,date});} date(value){return typeof value==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(value)&&!Number.isNaN(Date.parse(`${value}T00:00:00Z`));} bad(m){const e=new Error(m);e.status=400;return e;} }
module.exports={GetVacationMode};
