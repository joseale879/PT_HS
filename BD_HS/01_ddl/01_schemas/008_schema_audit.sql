-- ============================================================
-- ESQUEMA: audit (Auditoría y Logs)
-- ============================================================
CREATE SCHEMA IF NOT EXISTS audit;
COMMENT ON SCHEMA audit IS 'Auditoría y logs de errores';